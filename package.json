{
  "name": "three-webapp-framework",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/",
    "build:gh": "vite build --base=/CRxUAL-Virtual-Exhibiton-2026/",
    "preview": "vite preview",
    "deploy": "npm run build:gh && gh-pages -d dist",
    "deploy:gh": "npm run build:gh && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "gh-pages": "^6.3.0",
    "vite": "^6.4.2"
  }
}
